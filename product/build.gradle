dependencies {

    testCompile group: 'junit', name: 'junit', version: '4.12'
}

publishing {
    publications {
        product(MavenPublication) {
            artifactId "$project.name"
            artifact jar
        }
    }
}